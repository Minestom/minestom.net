# Serialization
Minestom serializes data through **[codecs](/docs/feature/serialization/codecs)**, which encode and decode to map-like formats such as JSON and NBT from a single definition. Use them for configuration files or data persistence.
